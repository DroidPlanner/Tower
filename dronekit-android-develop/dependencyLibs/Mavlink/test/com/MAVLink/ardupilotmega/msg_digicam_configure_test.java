/* AUTO-GENERATED FILE.  DO NOT MODIFY.
 *
 * This class was automatically generated by the
 * java mavlink generator tool. It should not be modified by hand.
 */
         
// MESSAGE DIGICAM_CONFIGURE PACKING
package com.MAVLink.ardupilotmega;
import com.MAVLink.MAVLinkPacket;
import com.MAVLink.Parser;
import com.MAVLink.ardupilotmega.CRC;
import java.nio.ByteBuffer;
import org.junit.Test;

import static org.junit.Assert.assertArrayEquals;

/**
* Configure on-board Camera Control System.
*/
public class msg_digicam_configure_test{

public static final int MAVLINK_MSG_ID_DIGICAM_CONFIGURE = 154;
public static final int MAVLINK_MSG_LENGTH = 15;
private static final long serialVersionUID = MAVLINK_MSG_ID_DIGICAM_CONFIGURE;

private Parser parser = new Parser();

public CRC generateCRC(byte[] packet){
    CRC crc = new CRC();
    for (int i = 1; i < packet.length - 2; i++) {
        crc.update_checksum(packet[i] & 0xFF);
    }
    crc.finish_checksum(MAVLINK_MSG_ID_DIGICAM_CONFIGURE);
    return crc;
}

public byte[] generateTestPacket(){
    ByteBuffer payload = ByteBuffer.allocate(6 + MAVLINK_MSG_LENGTH + 2);
    payload.put((byte)MAVLinkPacket.MAVLINK_STX); //stx
    payload.put((byte)MAVLINK_MSG_LENGTH); //len
    payload.put((byte)0); //seq
    payload.put((byte)255); //sysid
    payload.put((byte)190); //comp id
    payload.put((byte)MAVLINK_MSG_ID_DIGICAM_CONFIGURE); //msg id
    payload.putFloat((float)17.0); //extra_value
    payload.putShort((short)17443); //shutter_speed
    payload.put((byte)151); //target_system
    payload.put((byte)218); //target_component
    payload.put((byte)29); //mode
    payload.put((byte)96); //aperture
    payload.put((byte)163); //iso
    payload.put((byte)230); //exposure_type
    payload.put((byte)41); //command_id
    payload.put((byte)108); //engine_cut_off
    payload.put((byte)175); //extra_param
    
    CRC crc = generateCRC(payload.array());
    payload.put((byte)crc.getLSB());
    payload.put((byte)crc.getMSB());
    return payload.array();
}

@Test
public void test(){
    byte[] packet = generateTestPacket();
    for(int i = 0; i < packet.length - 1; i++){
        parser.mavlink_parse_char(packet[i] & 0xFF);
    }
    MAVLinkPacket m = parser.mavlink_parse_char(packet[packet.length - 1] & 0xFF);
    byte[] processedPacket = m.encodePacket();
    assertArrayEquals("msg_digicam_configure", processedPacket, packet);
}
}
        