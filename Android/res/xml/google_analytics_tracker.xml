<?xml version='1.0' encoding='UTF-8'?>
<!--
    Configuration parameters for the google analytics tracker.
    Reference: https://developers.google.com/analytics/devguides/collection/android/v4/parameters
--><resources xmlns:tools="http://schemas.android.com/tools" tools:ignore="TypographyDashes">

    <!-- Google Analytics tracking id -->
    <!-- Frequency of automatic dispatch in seconds -->
    <!-- Amount of time the app can stay in background before the session is ended -->
    <!-- Enable automatic Activity measurement -->
    <!-- Enable uncaught exception tracking -->
    <!-- The screen names that will show up in the report -->
    <screenName name="org.droidplanner.android.activities.FlightActivity">
        Flight Screen
    </screenName>

    <screenName name="org.droidplanner.android.activities.EditorActivity">
        Editor Screen
    </screenName>

    <screenName name="org.droidplanner.android.activities.ConfigurationActivity">
        Configuration Screen
    </screenName>

    <screenName name="org.droidplanner.android.activities.SettingsActivity">
        Settings Screen
    </screenName>

    <screenName name="org.droidplanner.android.activities.helpers.MapPreferencesActivity">
        Map Preferences Screen
    </screenName>
</resources>